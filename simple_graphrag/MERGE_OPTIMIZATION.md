# SimpleGraphRAG 合并优化说明

## 优化目标

优化 SimpleGraphRAG 的最后一个"合并到现有 system/graph"的阶段，确保：

1. **提取阶段可以并行执行**：多个任务同时进行 System 更新和实体关系提取
2. **合并阶段必须串行执行**：确保合并质量和数据一致性
3. **清晰的进度通知**：用户可以看到任务在哪个阶段、是否在等待合并

## 优化架构

### 两阶段任务处理

```
┌─────────────────────────────────────────────────────────────────┐
│                    阶段1: 提取阶段（可并行）                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Worker 1 (并行)     Worker 2 (并行)     Worker 3 (并行)       │
│     ↓                   ↓                   ↓                  │
│  任务A:              任务B:              任务C:                  │
│  • System更新       • System更新       • System更新            │
│  • 提取实体         • 提取实体         • 提取实体              │
│  • 提取关系         • 提取关系         • 提取关系              │
│  • 生成Delta        • 生成Delta        • 生成Delta             │
│     ↓                   ↓                   ↓                  │
└─────┼───────────────────┼───────────────────┼──────────────────┘
      └───────────────────┴───────────────────┘
                          ↓
              ┌────────────────────────┐
              │   合并队列 (FIFO)       │
              │  [任务A, 任务B, 任务C]  │
              └────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────────┐
│                    阶段2: 合并阶段（串行）                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│               Merge Worker (单线程)                             │
│                      ↓                                          │
│               逐个处理任务:                                       │
│               1. 任务A 合并 → 完成                               │
│               2. 任务B 合并 → 完成                               │
│               3. 任务C 合并 → 完成                               │
│                      ↓                                          │
│               智能合并流程:                                       │
│               • LLM 分析增量                                     │
│               • 去重和对齐                                       │
│               • 冲突解决                                         │
│               • 应用到主 system/graph                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 关键改进

### 1. 双队列设计

**之前（单队列）：**

```python
task_queue: asyncio.Queue  # 所有任务
# Workers 处理完直接合并（带锁）
```

**现在（双队列）：**

```python
task_queue: asyncio.Queue   # 提取阶段的任务队列
merge_queue: asyncio.Queue  # 合并阶段的任务队列
```

### 2. Worker 分离

**提取 Workers（多个，可并行）：**

- 数量：`max_concurrent_tasks` 个（如 3 个）
- 职责：执行 System 更新和实体关系提取
- 完成后：将任务放入 `merge_queue`

**合并 Worker（单个，串行）：**

- 数量：固定 **1 个**
- 职责：从 `merge_queue` 逐个取出任务进行智能合并
- 保证：合并操作串行执行，确保数据一致性

### 3. 进度通知优化

新增了更详细的进度阶段：

| 阶段 | 图标 | 百分比 | 说明 |
|------|------|--------|------|
| `started` | ▶️ | 0% | 任务开始 |
| `system_update` | 🔧 | 10-30% | System 更新阶段 |
| `extraction` | 🔍 | 50-80% | 实体关系提取阶段 |
| `extraction_completed` | 📦 | 90% | **提取完成，等待合并** |
| `merging` | 🔄 | 95% | **正在合并到主图谱** |
| `completed` | ✅ | 100% | 完全完成 |
| `merge_failed` | ⚠️ | 95% | 合并失败 |
| `failed` | ❌ | - | 任务失败 |
| `cancelled` | ⏹️ | - | 任务取消 |

### 4. 代码变更摘要

#### `simplegraph.py`

**初始化部分：**

```python
# 新增合并队列
self.merge_queue: asyncio.Queue = asyncio.Queue()

# 新增合并worker引用
self._merge_worker_task: Optional[asyncio.Task] = None
```

**启动逻辑：**

```python
async def start(self):
    # 启动多个提取workers
    for i in range(self.max_concurrent_tasks):
        worker = asyncio.create_task(self._worker(worker_id=i))
        self._worker_tasks.append(worker)
    
    # 启动单个合并worker
    self._merge_worker_task = asyncio.create_task(self._merge_worker())
```

**Worker 逻辑：**

```python
async def _worker(self, worker_id: int):
    # 执行提取任务
    delta = await self._run_task(task)
    
    # 完成后放入合并队列（不直接合并）
    await self.merge_queue.put(task)
```

**新增合并 Worker：**

```python
async def _merge_worker(self):
    """串行处理合并任务"""
    while self._running:
        # 从合并队列获取任务
        task = await self.merge_queue.get()
        
        # 通知开始合并
        self._notify_progress(task.task_id, "merging", {...})
        
        # 执行合并（串行，无需加锁）
        await self._auto_merge(task)
        
        # 通知完成
        self._notify_progress(task.task_id, "completed", {...})
```

## 性能优势

### 吞吐量提升

**场景：处理 10 个任务**

假设：

- 提取阶段耗时：30 秒/任务
- 合并阶段耗时：5 秒/任务

**优化前（所有阶段串行）：**

```
总耗时 = 10 × (30 + 5) = 350 秒
```

**优化后（提取并行3个，合并串行）：**

```
提取耗时 = ceil(10/3) × 30 = 120 秒  (4批次)
合并耗时 = 10 × 5 = 50 秒
总耗时 = 120 + 50 = 170 秒

提升：(350-170)/350 = 51% 更快！
```

### 质量保证

1. **数据一致性**：合并串行执行，避免并发写入冲突
2. **合并质量**：LLM 可以准确分析当前图谱状态
3. **顺序保证**：任务按完成顺序合并，符合预期

## 使用示例

### 基本使用

```python
import asyncio
from pathlib import Path
from simplegraph import SimpleGraph

def progress_callback(task_id: str, step: str, data: dict):
    """进度回调"""
    icons = {
        "extraction_completed": "📦",
        "merging": "🔄",
        "completed": "✅",
    }
    icon = icons.get(step, "•")
    message = data.get("message", "")
    percentage = data.get("percentage", 0)
    print(f"{icon} {task_id[:8]} | {step}: {message} ({percentage}%)")

async def main():
    # 初始化
    sg = SimpleGraph(
        config_path=Path("config/config.yaml"),
        max_concurrent_tasks=3,  # 3个提取workers
        enable_smart_merge=True,
        progress_callback=progress_callback,
    )
    
    await sg.start()
    
    # 提交多个任务（并行提取）
    task_ids = []
    for text in texts:
        task_id = await sg.submit_task(text)
        task_ids.append(task_id)
    
    # 等待完成
    while True:
        statuses = [sg.get_task_status(tid)["status"] for tid in task_ids]
        if all(s in ["completed", "failed"] for s in statuses):
            break
        await asyncio.sleep(0.5)
    
    await sg.stop()

asyncio.run(main())
```

### 观察到的进度输出

```
▶️ abc12345 | started: 任务已开始 (0%)
🔧 abc12345 | system_update: System更新完成 (30%)
🔍 abc12345 | extraction: 提取完成 (80%)
📦 abc12345 | extraction_completed: 提取阶段完成，等待合并 (90%)
🔄 abc12345 | merging: 开始智能合并到主图谱 (95%)
✅ abc12345 | completed: 任务已完成并合并 (100%)
```

## 技术细节

### 为什么合并必须串行？

1. **LLM 分析需要当前状态**：
   - 合并时 LLM 需要分析当前图谱的完整状态
   - 如果并行合并，LLM 看到的状态可能不一致

2. **避免竞态条件**：
   - 多个任务可能提取相同的实体（如"我"）
   - 并行合并可能导致重复创建或属性冲突

3. **保证合并质量**：
   - 智能合并包括去重、对齐、冲突解决
   - 这些操作需要准确的当前状态

### 为什么提取可以并行？

1. **任务隔离**：
   - 每个任务使用独立的 System 副本
   - 提取过程不修改主 system/graph

2. **无依赖关系**：
   - 任务A的提取不依赖任务B的结果
   - 可以完全独立进行

3. **性能优势**：
   - 提取阶段是最耗时的（LLM调用）
   - 并行可以显著提升吞吐量

## 监控和调试

### 查看合并队列状态

```python
# 获取等待合并的任务数
merge_queue_size = sg.merge_queue.qsize()
print(f"等待合并的任务数: {merge_queue_size}")
```

### 识别瓶颈

1. **如果 `merge_queue` 持续增长**：
   - 说明合并速度 < 提取速度
   - 考虑禁用 `enable_smart_merge` 或优化合并逻辑

2. **如果 `task_queue` 持续增长**：
   - 说明提取速度 < 任务提交速度
   - 考虑增加 `max_concurrent_tasks`

### 日志输出

```
[INFO] 启动 3 个提取workers和1个合并worker...
[INFO] Worker 0 启动
[INFO] Worker 1 启动
[INFO] Worker 2 启动
[INFO] Merge Worker 启动（串行处理）
[INFO] Worker 0 提取完成，任务进入合并队列: abc12345...
[INFO] Merge Worker 开始合并任务: abc12345...
[INFO] 智能合并完成: 2 duplicates, 1 conflicts, 3 aligned...
[INFO] 任务结果已合并到主图谱: abc12345...
[INFO] Merge Worker 任务合并完成: abc12345...
```

## 注意事项

1. **合并 Worker 是瓶颈**：
   - 由于串行执行，合并速度决定了最终吞吐量
   - 如果合并过慢，考虑简化合并逻辑或禁用智能合并

2. **内存使用**：
   - `merge_queue` 中的任务会占用内存
   - 如果提取速度远大于合并速度，队列会积压

3. **错误处理**：
   - 合并失败不会影响其他任务
   - 失败的任务会标记为 `merge_failed` 状态

## 未来优化方向

1. **批量合并**：
   - 将多个小任务的增量合并为一批
   - 减少 LLM 调用次数

2. **智能调度**：
   - 优先合并相关性低的任务
   - 延迟合并相关性高的任务，减少冲突

3. **可配置策略**：
   - 允许用户选择合并策略（严格/宽松）
   - 提供快速模式（跳过智能合并）

## 总结

这次优化通过**两阶段架构**，实现了：

- ✅ 提取阶段充分并行，提高吞吐量
- ✅ 合并阶段串行执行，保证质量
- ✅ 清晰的进度通知，用户体验更好
- ✅ 代码结构清晰，易于维护

在保证合并质量的前提下，显著提升了整体性能！
